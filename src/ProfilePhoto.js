import React from 'react';

import { config} from './Config';

const ProfilePhoto = (props) => {

    return (
        <div>
            
        </div>
    )
}

export default ProfilePhoto;